# Batched prefill: 1K input × N concurrent requests packed into scheduling
# chunks. Output is one token so the run stays focused on prefill while
# MoE-CAP still records the decode step separately if present.
dataset_names: ["longbench_v2"]
metrics: []
model_id: Qwen/Qwen1.5-MoE-A2.7B-Chat
precision: bfloat16
dataset_split: test
num_samples: 200

fixed_length_mode: true
target_input_tokens: 1024
target_output_tokens: 1
chunked_prefill_size: 131072
max_prefill_tokens: 131072
mem_fraction_static: 0.9
prefill_mode: batched
