# Prefill-heavy benchmark: 75% of 262K context = 196K input tokens, 1 output token
# Stresses prefill throughput / KV-cache fill with minimal decoding work.
# Uses LongBench v2 real documents as base text for realistic attention patterns.
dataset_names: ["longbench_v2"]
metrics: []
model_id: Qwen/Qwen3-Next-80B-A3B-Instruct
dataset_split: train

fixed_length_mode: true
target_input_tokens: 196608
target_output_tokens: 1
num_samples: 100
# Qwen3-Next-80B: KV=6KB/tok/GPU (TP=2), 80GB/GPU weights → max bs=4 at 196K input.
max_batch_size: 4
